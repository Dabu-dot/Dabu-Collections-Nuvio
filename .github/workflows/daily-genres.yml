name: Daily Genres Posters Generator

on:
  schedule:
    - cron: "0 5 * * *"  # S'exécute automatiquement tous les jours à 5h00 UTC
  workflow_dispatch:      # Permet un déclenchement manuel depuis l'interface GitHub

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  generate-posters:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pillow requests numpy

      - name: Run Posters Generator
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
          PYTHONDONTWRITEBYTECODE: "1"
        run: python3 .github/scripts/posters-genres.py

      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          # Indexation ciblée des posters et de l'historique trié
          git add "Ressources/Collections Covers/Genres/Static Covers/"
          git add ".github/scripts/posters_history.json"
          
          # Vérification intelligente : On commit et push uniquement si des fichiers ont bougé ou changé
          if ! git diff --cached --exit-code; then
            git commit -m "Mise à jour automatique des couvertures de genre [Skip CI]"
            git push origin main
          else
            echo "Aucun changement détecté (tous les posters existants ont été préservés ou reconduits)."
          fi
          
